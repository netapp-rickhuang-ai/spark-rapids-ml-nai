# Core requirements without cuml
pyspark==3.4.0
numpy>=1.20.0
pandas>=1.0.0
scikit-learn>=1.0.0  # As an alternative to cuml
```

Then install with:

```bash
pipenv install -r python/requirements_no_cuml.txt
